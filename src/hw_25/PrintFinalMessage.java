package hw_25;

public class PrintFinalMessage {

    public static void printMessage(final String message) {
        System.out.println("Message: " + message);
    }
    //message = "NTrcn";

    // message обьявлен как final , поэтому его значение не может быть изменено внутри метода
    // можно инициализировать только один раз

}
